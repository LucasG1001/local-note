import Layout from './components/layout/Layout';
import NoteManager from './note/NoteManager';

const Home = () => {
  return (
    <Layout>
      <NoteManager />
    </Layout>
  );
};

export default Home;
